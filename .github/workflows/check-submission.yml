name: Check Submission

on: [pull_request]

jobs:
  check-submission:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Base Repository
      uses: actions/checkout@v2
      with:
        ref: ${{ github.event.pull_request.base.ref }}
        path: 'base_repo'

    - name: Checkout Head Repository
      uses: actions/checkout@v2
      with:
        path: 'head_repo'

    - name: Check for Single File Addition in Submissions
      run: |
        # Count added files in the submissions directory
        cd head_repo
        added_files=$(git diff --name-status --diff-filter=A "origin/${{ github.event.pull_request.base.ref }}" | grep -c '^A\s\+submissions/')
        echo "Added files count: $added_files"
        if [ "$added_files" -ne "1" ]; then
          echo "Error: PR must add exactly one file to 'submissions/'"
          exit 1
        fi